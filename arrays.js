const nimed = ["Jüri", "Katrin", "Urmet", "Vello", "Jane"];
const vanused = [24, 22, 21, 35, 42];

const esimene = nimed[0];

const length = nimed.length;
console.log(length); // 5

// search
console.log(nimed.indexOf("Katrin")); // 1
console.log(nimed.indexOf("Jane")); // 4
console.log(nimed.indexOf("Nikolas")); // -1